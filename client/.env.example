# Point the client at a running eCampus ELO Portal API.
# Leave this unset (or empty) to run the portal in self-contained Demo Mode,
# which serves a full sample academic year from the browser.
VITE_API_URL=http://localhost:5000/api
